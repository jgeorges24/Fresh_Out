<H1> NESTED OPINIONS FORM </H1

<br>

<%= link_to 'View All Mixtapes', tapes_path %> 
<br>
# add the opinions on this page below


<p> this is the opinion form page </p>


<%= form_with(model:[@tape, @opinion]) do |f| %>
    <%= f.hidden_field :user_id, value: current_user.id %>
    <%= f.hidden_field :tape_id, value: @tape.id %>
    <%= f.text_area :context %>
    
    <%= f.submit :opinion %>
<% end %>